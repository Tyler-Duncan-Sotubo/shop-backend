import { BaseController } from "../../../infrastructure/interceptor/base.controller";
import { User } from "../common/types/user.type";
import { CampaignService } from "../../../domains/campaigns/services/campaigns.service";
import { CampaignSendService } from "../../../domains/campaigns/services/campaign-send.service";
import { CampaignAudienceService } from "../../../domains/campaigns/services/campaign-audience.service";
import { CreateCampaignDto, UpdateCampaignDto, ScheduleCampaignDto, SendTestDto, ListCampaignsDto, AudienceCountDto } from './dto/campaign.dto';
export declare class CampaignsController extends BaseController {
    private readonly campaignService;
    private readonly campaignSendService;
    private readonly audienceService;
    constructor(campaignService: CampaignService, campaignSendService: CampaignSendService, audienceService: CampaignAudienceService);
    create(user: User, body: CreateCampaignDto): Promise<{
        id: string;
        createdAt: Date;
        updatedAt: Date;
        companyId: string;
        storeId: string;
        channel: "email" | "sms";
        templateType: "new_arrival" | "promotion" | "newsletter";
        status: "draft" | "scheduled" | "sending" | "sent" | "failed";
        audienceType: "all" | "customers" | "subscribers";
        subject: string;
        previewText: string | null;
        contentJson: string | null;
        scheduledAt: Date | null;
        sentAt: Date | null;
        sentCount: number;
        openCount: number;
        clickCount: number;
        unsubscribeCount: number;
        resendBatchId: string | null;
    }>;
    list(user: User, q: ListCampaignsDto): Promise<{
        rows: {
            id: string;
            createdAt: Date;
            updatedAt: Date;
            companyId: string;
            storeId: string;
            channel: "email" | "sms";
            templateType: "new_arrival" | "promotion" | "newsletter";
            status: "draft" | "scheduled" | "sending" | "sent" | "failed";
            audienceType: "all" | "customers" | "subscribers";
            subject: string;
            previewText: string | null;
            contentJson: string | null;
            scheduledAt: Date | null;
            sentAt: Date | null;
            sentCount: number;
            openCount: number;
            clickCount: number;
            unsubscribeCount: number;
            resendBatchId: string | null;
        }[];
        count: number;
        limit: number;
        offset: number;
    }>;
    getById(user: User, id: string): Promise<{
        id: string;
        createdAt: Date;
        updatedAt: Date;
        companyId: string;
        storeId: string;
        channel: "email" | "sms";
        templateType: "new_arrival" | "promotion" | "newsletter";
        status: "draft" | "scheduled" | "sending" | "sent" | "failed";
        audienceType: "all" | "customers" | "subscribers";
        subject: string;
        previewText: string | null;
        contentJson: string | null;
        scheduledAt: Date | null;
        sentAt: Date | null;
        sentCount: number;
        openCount: number;
        clickCount: number;
        unsubscribeCount: number;
        resendBatchId: string | null;
    }>;
    update(user: User, id: string, body: UpdateCampaignDto): Promise<{
        id: string;
        createdAt: Date;
        updatedAt: Date;
        companyId: string;
        storeId: string;
        channel: "email" | "sms";
        templateType: "new_arrival" | "promotion" | "newsletter";
        status: "draft" | "scheduled" | "sending" | "sent" | "failed";
        audienceType: "all" | "customers" | "subscribers";
        subject: string;
        previewText: string | null;
        contentJson: string | null;
        scheduledAt: Date | null;
        sentAt: Date | null;
        sentCount: number;
        openCount: number;
        clickCount: number;
        unsubscribeCount: number;
        resendBatchId: string | null;
    }>;
    delete(user: User, id: string): Promise<{
        success: boolean;
    }>;
    schedule(user: User, id: string, body: ScheduleCampaignDto): Promise<{
        id: string;
        createdAt: Date;
        updatedAt: Date;
        companyId: string;
        storeId: string;
        channel: "email" | "sms";
        templateType: "new_arrival" | "promotion" | "newsletter";
        status: "draft" | "scheduled" | "sending" | "sent" | "failed";
        audienceType: "all" | "customers" | "subscribers";
        subject: string;
        previewText: string | null;
        contentJson: string | null;
        scheduledAt: Date | null;
        sentAt: Date | null;
        sentCount: number;
        openCount: number;
        clickCount: number;
        unsubscribeCount: number;
        resendBatchId: string | null;
    }>;
    unschedule(user: User, id: string): Promise<{
        id: string;
        createdAt: Date;
        updatedAt: Date;
        companyId: string;
        storeId: string;
        channel: "email" | "sms";
        templateType: "new_arrival" | "promotion" | "newsletter";
        status: "draft" | "scheduled" | "sending" | "sent" | "failed";
        audienceType: "all" | "customers" | "subscribers";
        subject: string;
        previewText: string | null;
        contentJson: string | null;
        scheduledAt: Date | null;
        sentAt: Date | null;
        sentCount: number;
        openCount: number;
        clickCount: number;
        unsubscribeCount: number;
        resendBatchId: string | null;
    }>;
    sendNow(user: User, id: string): Promise<{
        success: boolean;
        sentCount: number;
    }>;
    sendTest(user: User, id: string, body: SendTestDto): Promise<{
        success: boolean;
        sentTo: string;
    }>;
    audienceCount(user: User, q: AudienceCountDto): Promise<number>;
}
